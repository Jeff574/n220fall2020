var x = 50;
var y = 50;
var mouseX = x;
var mouseY = y;
moveUp=

function setup() {
  createCanvas(400, 300);
}

function draw() {
  background(153, 204, 255);
  fill(102, 255, 102);
  ellipse(mouseX, mouseY, 50, 50);

  if()
  x = x + 1;
}
